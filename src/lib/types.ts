import type { WorkExperience } from '$lib/types/WorkExperience';
import type { LocaleDictionary } from '$lib/types/LocalDictionary';
import type { Post } from '$lib/types/Post';
import type { Categories } from '$lib/types/Categories';
import type { Education } from '$lib/types/Education';
import type { Projects } from '$lib/types/Projects';
import type { Contact, ContactMethods } from '$lib/types/Contact';
import type { Navbar } from '$lib/types/Navbar';
import type { ResumeValue } from '$lib/types/ResumeValue';
import type { TranslatableValue } from '$lib/types/TranslatableValue';
import type { TranslatedValue } from '$lib/types/TranslatedValue';
import type { Resume } from '$lib/types/Resume';
import type { Customers } from '$lib/types/Customers';
import type { Testimonials } from '$lib/types/Testimonials';

export type {
	WorkExperience,
	LocaleDictionary,
	Post,
	Categories,
	Education,
	Projects,
	Contact,
	ContactMethods,
	Navbar,
	ResumeValue,
	TranslatableValue,
	TranslatedValue,
	Resume,
	Customers,
	Testimonials
};
